import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.switchnow.workerapp',
  appName: 'Switch',
  webDir: 'out',
  server: {
    // Points to your running Next.js server
    // Use 10.0.2.2 for Android emulator, or your local IP for a real phone
    url: 'http://192.168.1.108:3002',
    cleartext: true,
  },
  android: {
    backgroundColor: '#000000',
  },
}

export default config
