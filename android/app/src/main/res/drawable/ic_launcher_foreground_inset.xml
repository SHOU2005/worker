<?xml version="1.0" encoding="utf-8"?>
<!-- Wraps the mipmap foreground in an inset so it respects the 66/108 dp
     adaptive-icon safe zone. Without this the rounded-square + "Switch"
     wordmark in ic_launcher_foreground extends past the safe zone and the
     launcher's circular / squircle mask clips its corners. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@mipmap/ic_launcher_foreground"
    android:insetLeft="22dp"
    android:insetRight="22dp"
    android:insetTop="22dp"
    android:insetBottom="22dp" />
