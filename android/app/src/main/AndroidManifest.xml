<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme"
        tools:replace="android:usesCleartextTraffic">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- FCM: tells the messaging SDK which channel to auto-register so
             the urgent_ring sound (res/raw/urgent_ring.wav) actually plays
             on Android 8+ when the server pushes sound:"urgent_ring". -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="switch-urgent" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <!-- Native FCM handler — required so a `data`-only urgent push fires
             our full-screen-intent ring activity even when the JS service
             worker is asleep. -->
        <service
            android:name=".UrgentRingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Foreground service that holds the ringtone MediaPlayer + a
             wake lock so the phone keeps ringing until the worker accepts
             or declines. `mediaPlayback` type fits our looping ringtone
             and doesn't require dialer role like `phoneCall` does on
             Android 14+. -->
        <service
            android:name=".UrgentRingForegroundService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <!-- The native Accept/Decline call card (IncomingJobActivity) was
             removed — the web dashboard's own flashing urgent-job UI is
             enough, and the full-screen intent now points at MainActivity
             directly so the worker lands in the WebView at the job URL.
             Source files in /java/.../IncomingJobActivity.kt + the layout
             are kept on disk in case we revive the call-card flow. -->
    </application>

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location (for finding nearby jobs) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera (for KYC selfie and Aadhaar upload) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Storage (for uploading KYC documents) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Contacts (for referrals) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Urgent-job ring: wake the screen, show above lock screen, run a
         media-playback foreground service while ringing. Android 14+
         requires USE_FULL_SCREEN_INTENT — without it setFullScreenIntent
         silently falls back to a heads-up. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Jyoti voice assistant — getUserMedia({ audio: true }) inside the
         Capacitor WebView requires RECORD_AUDIO at the manifest level on
         Android 6+ so the runtime permission prompt can even appear. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

</manifest>
